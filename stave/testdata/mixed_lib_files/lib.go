// Some points here... we have a different package name for the non-stave code,
// and we need to make sure that we handle that.  We also have a duplicate
// funciotn name between the stave code and the non-stave code. That should also
// work correctly.

package lib

func Build() {]}
