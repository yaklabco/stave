// Some points here... we have a different package name for the non-staff code,
// and we need to make sure that we handle that.  We also have a duplicate
// function name between the staff code and the non-staff code. That should also
// work correctly.

package lib

func Build() {]}
